# Canopy Sight Web – copy to .env.local and fill in values
# Used by: apps/web (Next.js)

# ---- API (required for tRPC and health proxy) ----
NEXT_PUBLIC_API_URL="http://localhost:3001"

# ---- Optional: WebSocket URL (if different from API, e.g. ngrok) ----
# NEXT_PUBLIC_WS_URL="wss://your-api.fly.dev/socket.io"

# ---- Clerk (required for auth) ----
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_..."
CLERK_SECRET_KEY="sk_..."

# ---- Optional: ngrok (dev only – for chunk URLs when using ngrok URL) ----
# NGROK_URL="https://xxx.ngrok-free.dev"
